import 'package:flutter/material.dart';

/// Bluesky color constants
///
/// Color palette matching Bluesky's brand identity for crosspost UI elements.
class BlueskyColors {
  // Private constructor to prevent instantiation
  BlueskyColors._();

  /// Background color - dark blue-gray
  static const background = Color(0xFF161E27);

  /// Card border color - medium blue-gray
  static const cardBorder = Color(0xFF2E3D4F);

  /// Primary text color - white
  static const textPrimary = Color(0xFFFFFFFF);

  /// Secondary text color - light blue-gray
  static const textSecondary = Color(0xFF8B98A5);

  /// Bluesky brand blue
  static const blueskyBlue = Color(0xFF1185FE);

  /// Disabled/muted action color
  static const actionDisabled = Color(0xFF5C6E7E);
}
